## 01_fcc_thermo_multi

- **Structure**: FCC lattice (`lattice fcc`)
- **Thermo**: `thermo_style multi`
- **Multiple runs**: `run 0`, then two `run` segments with different NVT fixes

Outputs are written to `out/` by the launcher script.


